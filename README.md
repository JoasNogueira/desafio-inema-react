# Desafio Varal das Águas

#### Instruções:

1. Instalando as dependências:

   Na pasta raiz do projeto execute o comando abaixo no terminal para instalar as dependências:
   
   `npm install`
   
   
2. Iniciando a aplicação:
   
   `npm start`
   
